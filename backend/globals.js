/*
 * CONSTANTS
 */
const paths = {
  ROOT: 'backend',
  IMAGES: 'images',
  POSTS: 'posts',
  USERS: 'users',
  THUMBNAILS: 'thumbnails'
} // All project paths

const api = {
  POSTS: '/api/posts',
  USERS: '/api/users',
} // All API routes

module.exports = {
  CONSTANTS: {
    PATHS: paths,
    API: api
  }
}; // Export Globals