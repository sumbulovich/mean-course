import { MustMatchDirective } from './match-fields.directive';

describe('MustMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new MustMatchDirective();
    expect(directive).toBeTruthy();
  });
});
