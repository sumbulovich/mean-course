import { ResetFieldDirective } from '../reset-field.directive';

describe('ResetFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetFieldDirective();
    expect(directive).toBeTruthy();
  });
});
